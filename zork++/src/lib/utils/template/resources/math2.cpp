module math;
// Implementation of the definitions on the module unit interface
// for the subtract and divide math operations
namespace math {
    int subtract(int num1, int num2) {
        return num1 - num2;
    }
    int divide(int num1, int num2) {
        return num1 / num2;
    }
}
