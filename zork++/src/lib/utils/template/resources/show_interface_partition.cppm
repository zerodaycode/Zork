export module show_partitions:show_interface_partition;

// Note that we are ommiting the namespace here, as they are
// completly orthogonal to any feature of the modules
export int just_a_42() {
    return 42;
}