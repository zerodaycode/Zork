#This file it's autogenerated as an example of a Zork config file
[project]
name = "github_example"
authors = [ "Zero Day Code" ]  # Replace this for the real authors

[compiler]
cpp_compiler = "clang"
cpp_standard = "20"
std_lib = "libc++"  # This concrete property will only be applied to Clang

[build]
output_dir = "./out"

[executable]
executable_name = "github_example"
sources = [
    "./github_example/*.cpp"
]
auto_execute = true

[tests]
tests_executable_name = "zork_proj_tests"
sources = [
    "./github_example/*.cpp"
]

[modules]
base_ifcs_dir = "./github_example/ifc"
interfaces = [ 
    { file = 'commons/math.cppm'}  
]
base_impls_dir = "./github_example/src"
implementations = [
    { file = 'math.cpp' },
    { file = 'math2.cpp', dependencies = ['math'] }
]
sys_modules = ['iostream']