#This file it's autogenerated as an example of a Zork config file
[project]
name = "clang_example"
authors = [ "Zero Day Code" ]  # Replace this for the real authors

[compiler]
cpp_compiler = "clang"
cpp_standard = "20"
std_lib = "libc++"  # This concrete property will only be applied to Clang

[build]
output_dir = "./out"

[executable]
executable_name = "clang_example"
sources = [
    "./clang_example/*.cpp"
]
auto_execute = true

[tests]
tests_executable_name = "zork_proj_tests"
sources = [
    "./clang_example/*.cpp"
]

[modules]
base_ifcs_dir = "./clang_example/ifc"
interfaces = [ 
    { file = 'math.cppm' },   
    { file = 'interface_partition.cppm', module_name = 'partitions', partition_name = 'interface_partition' },
    { file = 'internal_partition.cpp', module_name = 'partitions', partition_name = 'internal_partition' },
    { file = 'partitions.cppm' }
]
base_impls_dir = "./clang_example/src"
implementations = [
    { file = 'math.cpp' },
    { file = 'math2.cpp', dependencies = ['math'] },
]
sys_modules = ['iostream']