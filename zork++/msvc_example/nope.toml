#This file it's autogenerated as an example of a Zork config file
[project]
name = "msvc_example"
authors = [ "Zero Day Code" ]  # Replace this for the real authors

[compiler]
cpp_compiler = "clang"
cpp_standard = "20"
std_lib = "libc++"  # This concrete property will only be applied to Clang

[build]
output_dir = "./out"

[executable]
executable_name = "msvc_example"
sources = [
    "./msvc_example/*.cpp"
]
auto_execute = true

[tests]
tests_executable_name = "zork_proj_tests"
sources = [
    "./msvc_example/*.cpp"
]

[modules]
base_ifcs_dir = "./msvc_example/ifc"
interfaces = [ 
    # { file = 'math.ixx' },   
    { file = 'interface_partition.ixx' },
    { file = 'internal_partition.cpp', is_internal_partition = true, dependencies = ['interface_partition'] },
    { file = 'partitions.ixx', dependencies = ['interface_partition'] }
]
base_impls_dir = "./msvc_example/src"
implementations = [
    # { file = 'math.cpp' },
    # { file = 'math2.cpp', dependencies = ['math'] },
]
sys_modules = ['iostream']